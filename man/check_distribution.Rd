% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_distribution.R
\name{check_distribution}
\alias{check_distribution}
\title{Check distribution and suggest a statistical approach}
\usage{
check_distribution(
  data,
  vars = NULL,
  by = NULL,
  normality_test = TRUE,
  digits = 2,
  output = c("table", "tibble", "both"),
  quiet = FALSE
)
}
\arguments{
\item{data}{A data.frame.}

\item{vars}{Optional character vector of variables to assess. By default, all
variables are considered and continuous variables are selected internally.}

\item{by}{Optional grouping variable. Can be supplied as a bare name or
character string. The grouping variable must be categorical, binary, or
ordinal.}

\item{normality_test}{Logical; whether to run the Shapiro-Wilk test when
feasible. The test is only run when the number of non-missing observations
is between 3 and 5000.}

\item{digits}{Number of decimal places used when formatting numeric output.}

\item{output}{Output style. Currently stored in the returned object.}

\item{quiet}{Logical; suppress messages.}
}
\value{
A \code{gt_distribution} object containing:
\itemize{
\item \code{inputs} — function inputs and settings
\item \code{summary} — detailed summary table with raw statistics
\item \code{table} — display-ready table
\item \code{notes} — guidance notes
\item \code{call} — matched function call
}
}
\description{
Assess the distribution of continuous variables and provide practical
guidance on suitable summary measures and common inferential tests.
}
\details{
This function is designed for teaching and applied statistical workflows. It
focuses on continuous variables and summarises distributional features:
skewness, optional Shapiro-Wilk p-values, and recommended choices for:
\itemize{
\item descriptive summaries
\item two-group tests
\item multi-group tests
}

By default, all continuous variables in the dataset are assessed. You may
also restrict the analysis to selected variables using \code{vars}. If a grouping
variable is supplied through \code{by}, distributional summaries are produced
separately for each group.

Distribution guidance is based primarily on skewness. When
\code{normality_test = TRUE}, Shapiro-Wilk p-values are also shown when feasible,
but these should be interpreted cautiously and not used in isolation.
}
\examples{
check_distribution(mtcars)

check_distribution(mtcars, vars = c("mpg", "wt", "disp"))

check_distribution(mtcars, by = am)

tbl_stats(check_distribution(mtcars))

}
