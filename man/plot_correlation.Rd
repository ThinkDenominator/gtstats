% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_correlation.R
\name{plot_correlation}
\alias{plot_correlation}
\title{Plot the relationship between two continuous variables}
\usage{
plot_correlation(
  data,
  x,
  y,
  method = c("auto", "pearson", "spearman"),
  trend = c("auto", "linear", "smooth", "none"),
  show_ci = TRUE,
  show_correlation = FALSE,
  conf.level = 0.95,
  digits = 2,
  point_color = "#4472C4",
  line_color = "#ED7D31",
  base_size = 14,
  title = NULL,
  caption = NULL,
  xlab = NULL,
  ylab = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{x, y}{Continuous variables, supplied as bare names or character strings.}

\item{method}{Correlation method: \code{"auto"}, \code{"pearson"}, or \code{"spearman"}.}

\item{trend}{Fitted trend: \code{"auto"}, \code{"linear"}, \code{"smooth"}, or \code{"none"}.}

\item{show_ci}{Logical; display the confidence band around a fitted trend.}

\item{show_correlation}{Logical; report the correlation result in the
caption.}

\item{conf.level}{Confidence level passed to \code{\link[=correlation]{correlation()}}.}

\item{digits}{Number of decimal places used in the correlation annotation.}

\item{point_color, line_color}{Colours used for observations and the trend.}

\item{base_size}{Base font size.}

\item{title, caption}{Optional plot title and caption.}

\item{xlab, ylab}{Optional axis labels.}
}
\value{
A \code{ggplot} object.
}
\description{
Create a publication-ready scatterplot that is aligned with
\code{\link[=correlation]{correlation()}}. The minimal call is \code{plot_correlation(data, x, y)}.
Incomplete pairs are excluded and the analysed number of complete pairs is
shown in the caption.
}
\details{
With \code{trend = "auto"}, a linear trend is used for Pearson correlation and a
smooth trend for Spearman correlation. Set \code{trend = "none"} to display the
observations alone. When \code{show_correlation = TRUE}, the caption reports the
same method, coefficient, confidence interval when available, and p-value as
\code{\link[=correlation]{correlation()}}. The returned object is a standard \code{ggplot}, so ordinary
ggplot2 layers can be added.
}
\examples{
plot_correlation(mtcars, x = mpg, y = wt)

plot_correlation(
  mtcars,
  x = mpg,
  y = wt,
  show_correlation = TRUE
)

}
