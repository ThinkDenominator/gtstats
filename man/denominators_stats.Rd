% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inspect-results.R
\name{denominators_stats}
\alias{denominators_stats}
\title{Inspect statistical denominators}
\usage{
denominators_stats(
  x,
  output = c("tibble", "gt"),
  title = "Denominator audit",
  subtitle = NULL,
  view = c("readable", "audit")
)
}
\arguments{
\item{x}{A \code{gtstats} result.}

\item{output}{Return a \code{"tibble"} or formatted \code{"gt"} table.}

\item{title, subtitle}{Optional table heading used for \code{output = "gt"}.}

\item{view}{Either \code{"readable"} (the default, plain-language headings) or
\code{"audit"} (raw field names retained by the analysis object).}
}
\value{
A tibble or \code{gt_tbl}.
}
\description{
Return a transparent audit of the observations, missing values, numerators,
denominators, and denominator rules used in a \code{gtstats} result.
}
\examples{
result <- proportion_stats(mtcars, var = vs, by = am)
denominators_stats(result)
}
