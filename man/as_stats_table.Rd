% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_stats_table.R
\name{as_stats_table}
\alias{as_stats_table}
\title{Convert an already summarised data frame into a publication table}
\usage{
as_stats_table(data, notes = NULL)
}
\arguments{
\item{data}{A data frame or tibble containing one row per intended table row.}

\item{notes}{Optional character vector of explanatory notes to display below
the table.}
}
\value{
A \code{gt_data_table} object. Use \code{\link[=customise_table]{customise_table()}}, \code{\link[=to_flextable]{to_flextable()}},
\code{\link[=to_gt]{to_gt()}}, or \code{\link[=save_output]{save_output()}} to present or export it.
}
\description{
Wrap a data frame or tibble containing values that have already been
calculated so it can be rendered, styled, and exported with gtstats. No
descriptive statistics, confidence intervals, or p-values are calculated or
checked by this function.
}
\details{
Use \code{\link[=summary_table]{summary_table()}} instead when each row represents a participant or
observation and descriptive statistics still need to be calculated. Use
\code{\link[=epi_table]{epi_table()}} when events and denominators need to be calculated from a line
list or aggregate outbreak/surveillance data.
}
\examples{
summarised <- mtcars |>
  dplyr::summarise(
    Cars = dplyr::n(),
    `Mean mpg` = mean(mpg),
    `Mean weight` = mean(wt)
  )

table <- as_stats_table(
  summarised,
  notes = "Values were calculated before table formatting."
)
customise_table(table, title = "Vehicle summary")
to_flextable(table)
to_gt(table)

}
