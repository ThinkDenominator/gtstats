% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlate_vars.R
\name{correlate_vars}
\alias{correlate_vars}
\title{Correlate two variables}
\usage{
correlate_vars(
  data,
  x,
  y,
  method = c("auto", "pearson", "spearman"),
  conf.level = 0.95,
  digits = 3,
  output = c("table", "tibble", "both"),
  quiet = FALSE
)
}
\arguments{
\item{data}{A data.frame.}

\item{x}{First variable. Can be supplied as a bare name or as a character
string.}

\item{y}{Second variable. Can be supplied as a bare name or as a character
string.}

\item{method}{Correlation method. One of \code{"auto"}, \code{"pearson"}, or
\code{"spearman"}.}

\item{conf.level}{Confidence level for the interval. Default is \code{0.95}.}

\item{digits}{Number of decimal places used when formatting numeric output.}

\item{output}{Output style. Currently stored in the returned object.}

\item{quiet}{Logical; suppress messages.}
}
\value{
A \code{gt_correlation} object containing:
\itemize{
\item \code{inputs} — function inputs and settings
\item \code{summary} — detailed summary table
\item \code{table} — display-ready table
\item \code{method} — metadata on variable types and method used
\item \code{notes} — explanatory notes
\item \code{call} — matched function call
}
}
\description{
Perform correlation analysis between two continuous variables using Pearson
or Spearman correlation.
}
\details{
This function is designed for practical teaching and applied analysis. It
accepts two continuous variables, removes incomplete pairs, selects a
correlation method automatically or uses a user-specified method, and returns
both a detailed summary and a display-ready table.

In \code{method = "auto"} mode:
\itemize{
\item Pearson correlation is used when both variables are approximately symmetric
\item Spearman correlation is used otherwise
}

The output includes the correlation estimate, confidence interval when
available, p-value, strength, direction, and a short interpretation.
}
\examples{
correlate_vars(mtcars, x = mpg, y = wt)

correlate_vars(mtcars, x = "mpg", y = "disp", method = "pearson")

tbl_stats(correlate_vars(mtcars, x = mpg, y = wt))

}
