% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_proportion.R
\name{add_proportion}
\alias{add_proportion}
\title{Add a proportion row to a descriptive table}
\usage{
add_proportion(
  x,
  var,
  level = NULL,
  ci = TRUE,
  conf.level = NULL,
  ci_method = NULL,
  display = NULL,
  layout = NULL,
  digits = NULL,
  label = NULL
)
}
\arguments{
\item{x}{A \code{gt_desc_table} object created with \code{\link[=summary_table]{summary_table()}}.}

\item{var}{Variable to summarise as a proportion. Can be supplied as a bare
name or as a character string.}

\item{level}{Optional level to count. If \code{NULL}, a default level is selected
automatically.}

\item{ci}{Logical; whether to display a confidence interval.}

\item{conf.level}{Confidence level for the interval. \code{NULL} inherits the
parent table setting, usually \code{0.95}.}

\item{ci_method}{Confidence-interval method: \code{"wilson"} or \code{"exact"}.
\code{NULL} inherits the parent table setting.}

\item{display}{Cell display: \code{"n_percent"}, \code{"percent"}, or
\code{"n_over_N_percent"}. \code{NULL} inherits the categorical display used by the
parent table.}

\item{layout}{Table layout. \code{NULL} inherits the parent table layout;
\code{"compact"} keeps the estimate and CI together and \code{"separate"} places
them in separate columns beneath each cohort header.}

\item{digits}{Number of decimal places used when formatting percentages.
\code{NULL} inherits the parent table precision.}

\item{label}{Optional row label. Defaults to the variable label if available,
otherwise the variable name.}
}
\value{
An updated \code{gt_desc_table} object with a proportion row appended.
}
\description{
Add a row showing the proportion of a selected level of a binary,
categorical, or ordinal variable within a \code{gtstats} descriptive table.
}
\details{
This is useful when you want to highlight a specific category such as
\code{"Yes"}, \code{"1"}, or \code{"TRUE"} within a Table 1 workflow. The row can be added
overall, by groups, or both, depending on how the descriptive table was
created.

If \code{level = NULL}, the function chooses a default level using the following
order:
\itemize{
\item \code{"1"}
\item \code{"Yes"} / \code{"yes"}
\item \code{"TRUE"} / \code{"True"} / \code{"true"}
\item the second available level for binary variables
\item otherwise the first available non-missing level
}

Wilson confidence intervals are used by default. Exact binomial intervals
are available with \code{ci_method = "exact"}.
}
\examples{
summary_table(mtcars, by = am, overall = TRUE) |>
  add_proportion(var = vs)

summary_table(mtcars, by = am, overall = TRUE) |>
  add_proportion(var = vs, level = "1", ci = TRUE)

summary_table(mtcars) |>
  add_proportion(var = vs, ci = FALSE)

}
