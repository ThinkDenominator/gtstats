% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prop_ci.R
\name{prop_ci}
\alias{prop_ci}
\title{Calculate proportion with confidence interval}
\usage{
prop_ci(
  data,
  var,
  by = NULL,
  level = NULL,
  conf.level = 0.95,
  digits = 1,
  output = c("table", "tibble", "both"),
  quiet = FALSE
)
}
\arguments{
\item{data}{A data.frame.}

\item{var}{Variable to summarise as a proportion. Can be supplied as
a bare name or as a character string.}

\item{by}{Optional grouping variable. Can be supplied as a bare name
or as a character string.}

\item{level}{Optional level to count. If \code{NULL}, the function will
try to use \code{"1"}, \code{"Yes"}, \code{"TRUE"}, or the second available
level.}

\item{conf.level}{Confidence level for the interval. Default is
\code{0.95}.}

\item{digits}{Number of decimal places used when formatting
percentages.}

\item{output}{Output style. Currently stored in the returned object.}

\item{quiet}{Logical; suppress messages.}
}
\value{
A \code{gt_prop} object containing:
\itemize{
\item \code{inputs} — function inputs and settings
\item \code{summary} — detailed summary table
\item \code{table} — display-ready table
\item \code{notes} — explanatory note
\item \code{call} — matched function call
}
}
\description{
Calculate the proportion of a selected level of a binary,
categorical, or ordinal variable, with optional grouping and exact
binomial confidence intervals.
}
\details{
This function is useful for simple epidemiological summaries and for
quickly reporting the proportion of a target category such as \code{"1"},
\code{"Yes"}, or \code{"TRUE"}. If \code{level = NULL}, a default level is chosen
automatically using a simple priority rule.

When a grouping variable is supplied, proportions are calculated
separately within each group. Confidence intervals are calculated
using \code{\link[stats:binom.test]{stats::binom.test()}}.
}
\examples{
prop_ci(mtcars, var = vs)

prop_ci(mtcars, var = vs, by = am)

prop_ci(mtcars, var = vs, by = am, level = "1")

tbl_stats(prop_ci(mtcars, var = vs, by = am))

}
