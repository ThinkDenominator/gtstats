% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rate_stats.R
\name{rate_stats}
\alias{rate_stats}
\title{Incidence rate with exact Poisson confidence interval}
\usage{
rate_stats(
  data,
  event,
  time,
  by = NULL,
  multiplier = 1000,
  time_label = NULL,
  conf.level = 0.95,
  digits = 1,
  format = c("table", "tibble")
)
}
\arguments{
\item{data}{A data.frame.}

\item{event}{Event variable. Can be supplied as a bare name or as a
character string. May be binary (\code{0/1}, \code{TRUE/FALSE}) or a count
variable.}

\item{time}{Person-time variable. Can be supplied as a bare name or
as a character string. Must be numeric and non-negative.}

\item{by}{Optional grouping variable. Can be supplied as a bare name
or as a character string.}

\item{multiplier}{Numeric multiplier used to scale the rate, for
example \code{1000} or \code{100000}. Default is \code{1000}.}

\item{time_label}{Optional readable unit for accumulated time, such as
\code{"person-years"} or \code{"catheter-days"}. Defaults to \code{"person-time"}.}

\item{conf.level}{Confidence level for the interval. Default is
\code{0.95}.}

\item{digits}{Number of decimal places used when formatting rates.
Default is \code{1}.}

\item{format}{Output format: \code{"table"} (default) or a plain console
\code{"tibble"}.}
}
\value{
A \code{gt_rate} object containing:
\itemize{
\item \code{inputs} — function inputs and settings
\item \code{summary} — detailed summary table
\item \code{table} — display-ready table
\item \code{notes} — explanatory note
\item \code{call} — matched function call
}
}
\description{
Compute an incidence or event rate with exact Poisson confidence
intervals, either overall or within groups defined by a categorical
variable.
}
\details{
This function is designed for simple epidemiological summaries where
events are counted over a denominator of person-time. The \code{event}
variable may be a binary indicator, a logical variable, or a count
variable. The \code{time} variable must be numeric, finite, and non-negative.
A zero accumulated person-time denominator is retained and clearly marked as
not estimable rather than silently converted to a rate.

When a grouping variable is supplied, rates are calculated
separately within each group. Confidence intervals are calculated
using \code{\link[stats:poisson.test]{stats::poisson.test()}}.
The publication table uses each group as a spanning header, with separate
columns for events, accumulated time, rate, and confidence interval. The
tidy long-form numerical results remain available in \verb{$summary}.
}
\examples{
df <- data.frame(
  event = c(1, 0, 1, 0, 1, 1),
  ptime = c(10, 12, 8, 9, 11, 7),
  arm = c("A", "A", "A", "B", "B", "B")
)

rate_stats(df, event = event, time = ptime)

rate_stats(df, event = event, time = ptime, by = arm)

to_gt(rate_stats(df, event = event, time = ptime, by = arm))

}
