% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effect_size_stats.R
\name{effect_size}
\alias{effect_size}
\title{Estimate an effect size}
\usage{
effect_size(
  data,
  outcome,
  by,
  method = c("auto", "hedges_g", "rank_biserial", "omega_squared", "epsilon_squared",
    "cramers_v"),
  paired = FALSE,
  id = NULL,
  conf.level = 0.95,
  interpretation = FALSE,
  digits = 2
)
}
\arguments{
\item{data}{A data frame.}

\item{outcome}{Outcome variable.}

\item{by}{Grouping variable.}

\item{method}{Effect-size method: \code{"auto"}, \code{"hedges_g"},
\code{"rank_biserial"}, \code{"omega_squared"}, \code{"epsilon_squared"}, or
\code{"cramers_v"}.}

\item{paired}{Logical; whether the two-group comparison is paired.}

\item{id}{Pair or participant identifier required when \code{paired = TRUE}.}

\item{conf.level}{Confidence level for supported intervals.}

\item{interpretation}{Logical; display a conventional magnitude label.
These labels are generic teaching aids and are not clinical importance
thresholds.}

\item{digits}{Number of decimal places.}
}
\value{
A publication-ready \code{gt_effect} object containing \code{summary}, \code{table},
\code{inputs}, \code{method}, \code{assumptions}, \code{diagnostics}, \code{denominators}, and
\code{notes}.
}
\description{
Quantify the magnitude of a group difference or association without adding
the full hypothesis-test output produced by \code{\link[=compare_groups]{compare_groups()}}.
For directional two-group measures, the Contrast column names the grouping
variable and reports first group minus second group. Factor order is therefore
meaningful. Cramer's V and omnibus measures have no direction.
}
\details{
The default \code{method = "auto"} selects one measure from the outcome and
comparison structure:
\itemize{
\item Hedges' g for two-group parametric comparisons
\item rank-biserial correlation for two-group rank comparisons
\item omega-squared for comparisons involving more than two continuous groups
\item epsilon-squared when a multi-group rank method is requested
\item Cramer's V for categorical associations
}

Risk ratios, odds ratios, and risk differences are intentionally not
duplicated here; use \code{\link[=crosstabs]{crosstabs()}} for those epidemiological measures.
}
\examples{
effect_size(mtcars, outcome = mpg, by = am)

effect_size(
  mtcars,
  outcome = mpg,
  by = am,
  method = "hedges_g",
  interpretation = TRUE
)

}
