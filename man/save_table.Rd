% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_table.R
\name{save_table}
\alias{save_table}
\title{Save a formatted table to file}
\usage{
save_table(
  x,
  filename,
  path = NULL,
  title = NULL,
  subtitle = NULL,
  pvalue_style = c("default", "scientific"),
  bold_labels = TRUE,
  show_footnotes = TRUE,
  zoom = 1.5,
  expand = 10,
  vwidth = 1400,
  vheight = 900,
  quiet = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{gtstats} object or a \code{gt_tbl}.}

\item{filename}{Output file name, including extension.}

\item{path}{Optional directory to save into. Defaults to
\code{tempdir()}.}

\item{title}{Optional title passed to \code{\link[=tbl_stats]{tbl_stats()}} when \code{x} is a
\code{gtstats} object.}

\item{subtitle}{Optional subtitle passed to \code{\link[=tbl_stats]{tbl_stats()}}.}

\item{pvalue_style}{P-value display style passed to \code{\link[=tbl_stats]{tbl_stats()}}.}

\item{bold_labels}{Logical; whether labels should be bolded in
\code{\link[=tbl_stats]{tbl_stats()}}.}

\item{show_footnotes}{Logical; whether footnotes should be shown in
\code{\link[=tbl_stats]{tbl_stats()}}.}

\item{zoom}{Scaling factor used for PNG export where supported.}

\item{expand}{Number of pixels to expand image borders for PNG
export.}

\item{vwidth}{Virtual browser width in pixels for PNG export where
supported.}

\item{vheight}{Virtual browser height in pixels for PNG export where
supported.}

\item{quiet}{Logical; suppress save message.}

\item{...}{Additional arguments passed to \code{\link[gt:gtsave]{gt::gtsave()}}.}
}
\value{
Invisibly returns the saved file path.
}
\description{
Save a \code{gtstats} object or a rendered \code{gt_tbl} to disk.
}
\details{
If a \code{gtstats} object is supplied, it is first rendered with
\code{\link[=tbl_stats]{tbl_stats()}}. If \code{path = NULL}, the file is saved to \code{tempdir()}
and the full path is reported in a message.

Supported formats depend on \code{\link[gt:gtsave]{gt::gtsave()}} and currently include:
HTML, PNG, PDF, RTF, LaTeX, and DOCX.
}
\examples{
res <- descriptive_table(mtcars, by = am, overall = TRUE) |>
  add_summary(vars = c(mpg, wt, cyl)) |>
  add_total() |>
  add_p()

\donttest{
save_table(res, filename = "table1.html")
}

gt_obj <- tbl_stats(res)

\donttest{
save_table(gt_obj, filename = "table2.html")
}

}
