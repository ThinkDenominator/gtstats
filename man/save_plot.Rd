% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_plot.R
\name{save_plot}
\alias{save_plot}
\title{Save a ggplot object to file}
\usage{
save_plot(
  plot,
  filename,
  path = NULL,
  width = 7,
  height = 5,
  units = c("in", "cm", "mm"),
  dpi = 300,
  bg = "white",
  quiet = FALSE,
  ...
)
}
\arguments{
\item{plot}{A \code{ggplot} object.}

\item{filename}{Output file name, including extension.}

\item{path}{Optional directory to save into. Defaults to
\code{tempdir()}.}

\item{width}{Width of the plot.}

\item{height}{Height of the plot.}

\item{units}{Units for width and height. One of \code{"in"},
\code{"cm"}, \code{"mm"}.}

\item{dpi}{Resolution in dots per inch for raster outputs.}

\item{bg}{Background colour passed to \code{ggsave()}.}

\item{quiet}{Logical; suppress save message.}

\item{...}{Additional arguments passed to
\code{ggplot2::ggsave()}.}
}
\value{
Invisibly returns the saved file path.
}
\description{
Save a \code{ggplot} object to disk using \code{ggplot2::ggsave()}.
}
\details{
If \code{path = NULL}, the file is saved to \code{tempdir()} and the full
path is reported in a message.

Supported formats include PNG, PDF, TIFF, JPEG, and SVG,
depending on the graphics device available.
}
\examples{
library(ggplot2)

p <- ggplot(mtcars, aes(x = wt, y = mpg)) +
  geom_point()

# Save to temporary directory
save_plot(p, filename = "example_plot.png")

# Save to a specific folder
\donttest{
save_plot(
  p,
  filename = "example_plot.pdf",
  path = tempdir(),
  width = 6,
  height = 4
)
}

}
