% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/to_flextable.R
\name{to_flextable}
\alias{to_flextable}
\title{Convert a gtstats object to flextable}
\usage{
to_flextable(x, font_size = 10, autofit = TRUE)
}
\arguments{
\item{x}{A supported \code{gtstats} object containing a \verb{$table}
component, such as an object returned by \code{\link[=descriptive_table]{descriptive_table()}},
\code{\link[=summary_stats]{summary_stats()}}, \code{\link[=compare_groups]{compare_groups()}}, \code{\link[=prop_ci]{prop_ci()}}, or related
functions.}

\item{font_size}{Font size applied to the whole table. Default is
\code{10}.}

\item{autofit}{Logical; whether column widths should be adjusted
automatically using \code{\link[flextable:autofit]{flextable::autofit()}}.}
}
\value{
A \code{flextable} object.
}
\description{
Convert a supported \code{gtstats} object into a \code{flextable} for use in
Word, PowerPoint, and other Office-style outputs.
}
\details{
This function is intended for export workflows where a \code{gtstats}
object needs to be inserted into a report or presentation. The input
should be the original analytical object, not the rendered \code{gt} table
returned by \code{\link[=tbl_stats]{tbl_stats()}}.

Text columns are left-aligned and value columns are right-aligned.
Notes, footnotes, and p-value method footnotes are appended to the
footer when available.
}
\examples{
res <- descriptive_table(mtcars, by = am, overall = TRUE) |>
  add_summary(vars = c(mpg, wt, cyl)) |>
  add_total() |>
  add_p()

to_flextable(res)

}
