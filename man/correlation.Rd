% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation-matrix.R
\name{correlation}
\alias{correlation}
\title{Correlation analysis for one pair or several continuous variables}
\usage{
correlation(
  data,
  x = NULL,
  y = NULL,
  method = c("auto", "pearson", "spearman"),
  conf.level = 0.95,
  digits = 2,
  vars = NULL,
  triangle = c("lower", "upper", "full"),
  order = c("input", "alphabetical", "cluster"),
  show_diagonal = TRUE,
  display = c("estimate", "estimate_p", "estimate_n", "estimate_p_n", "estimate_ci"),
  shade = TRUE,
  missing = c("pairwise"),
  adjust = c("none", "holm", "bonferroni", "BH"),
  format = c("table", "tibble")
)
}
\arguments{
\item{data}{A data frame.}

\item{x, y}{Two continuous variables supplied as bare names or character
strings. Omit these when using \code{vars}.}

\item{method}{Correlation method: \code{"auto"}, \code{"pearson"}, or \code{"spearman"}.}

\item{conf.level}{Confidence level for intervals.}

\item{digits}{Number of decimal places used for display.}

\item{vars}{Optional vector of at least two continuous variables, supplied as
\code{c(age, weight, outcome)} or a character vector.}

\item{triangle}{Matrix display: \code{"lower"}, \code{"upper"}, or \code{"full"}.}

\item{order}{Variable order in matrix mode: \code{"input"} preserves the order in
\code{vars}, \code{"alphabetical"} orders display labels, and \code{"cluster"} places
variables with similar absolute correlation patterns together.}

\item{show_diagonal}{Logical; show self-correlations on the diagonal.}

\item{display}{Matrix cell content: correlation \code{"estimate"},
\code{"estimate_p"}, \code{"estimate_n"}, \code{"estimate_p_n"}, or \code{"estimate_ci"}.
Confidence intervals unavailable
from the selected method are shown as an em dash in the tidy result and
omitted from the matrix cell.}

\item{shade}{Logical; apply coefficient-based shading to the publication
matrix. This affects rendering, not \verb{$summary}.}

\item{missing}{Matrix missing-data rule. Currently \code{"pairwise"}: each
coefficient uses all complete finite observations for that pair.}

\item{adjust}{Multiplicity adjustment for matrix p-values: \code{"none"},
\code{"holm"}, \code{"bonferroni"}, or \code{"BH"}.}

\item{format}{Output format: \code{"table"} (default) or a plain console
\code{"tibble"}.}
}
\value{
A \code{gt_correlation} object. Matrix results additionally inherit from
\code{gt_correlation_matrix} and contain a tidy pair-level \verb{$summary}.
}
\description{
\code{correlation()} analyses either one prespecified pair (\code{x} and \code{y}) or a
correlation matrix (\code{vars}). Matrix mode uses one method throughout, retains
pairwise sample sizes and inferential results in \verb{$summary}, and prints a
compact publication-ready matrix. Use \code{\link[=plot_correlation]{plot_correlation()}} for a shaded
heatmap of a matrix result.
}
\details{
In automatic matrix mode, Pearson correlation is used only when every
selected variable has absolute sample skewness below 1; otherwise Spearman
correlation is used throughout. This is transparent descriptive guidance,
not proof of linearity or monotonicity. Inspect the matrix heatmap and
relevant pairwise plots before interpretation.
}
\examples{
correlation(mtcars, x = mpg, y = wt)
correlation(mtcars, vars = c(mpg, disp, hp, wt))
plot_correlation(correlation(mtcars, vars = c(mpg, disp, hp, wt)))
}
