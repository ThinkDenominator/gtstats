% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_stats.R
\name{summary_stats}
\alias{summary_stats}
\title{Summary statistics}
\usage{
summary_stats(
  data,
  vars = NULL,
  by = NULL,
  type = NULL,
  include = c("auto", "continuous", "categorical", "binary", "ordinal"),
  continuous_format = c("recommended", "mean_sd", "median_iqr", "both"),
  recommend = TRUE,
  normality_check = FALSE,
  missing = c("ifany", "no", "always"),
  digits = 1,
  conf.level = 0.95,
  output = c("table", "tibble", "both"),
  quiet = FALSE
)
}
\arguments{
\item{data}{A data.frame.}

\item{vars}{Optional character vector of variables to summarise.
Default is all variables in \code{data}.}

\item{by}{Optional grouping variable. Can be supplied as a bare
name or as a character string.}

\item{type}{Optional named list used to override detected variable
types.}

\item{include}{Variable types to include. One of \code{"auto"},
\code{"continuous"}, \code{"categorical"}, \code{"binary"}, or \code{"ordinal"}.}

\item{continuous_format}{Format used for continuous summaries.}

\item{recommend}{Logical; whether to include a recommendation
column internally.}

\item{normality_check}{Logical; currently reserved for future
versions.}

\item{missing}{How to display missingness. One of \code{"ifany"},
\code{"no"}, or \code{"always"}.}

\item{digits}{Number of decimal places used for formatting.}

\item{conf.level}{Confidence level; currently reserved for future
versions.}

\item{output}{Output style.}

\item{quiet}{Logical; suppress messages.}
}
\value{
A \code{gt_summary} object containing:
\itemize{
\item \code{inputs} — function inputs and settings
\item \code{variable_info} — detected or overridden variable types
\item \code{summary} — detailed summary table
\item \code{table} — display-ready table
\item \code{method} — summary method description
\item \code{notes} — contextual notes
\item \code{call} — matched function call
}
}
\description{
Compute descriptive statistics for continuous and categorical
variables, with optional grouping.
}
\details{
This function is designed for practical descriptive analysis and
teaching workflows. It supports continuous, binary, categorical,
and ordinal variables, and produces both a detailed summary object
and a display-ready table.

Continuous variables can be summarised using:
\itemize{
\item \code{"recommended"}: mean (SD) or median (IQR) based on skewness
\item \code{"mean_sd"}: mean (SD)
\item \code{"median_iqr"}: median (IQR)
\item \code{"both"}: mean (SD) and median (IQR)
}

Categorical variables are summarised as count (\%).
}
\examples{
summary_stats(mtcars)

summary_stats(mtcars, by = am)

summary_stats(
  mtcars,
  vars = c("mpg", "wt", "cyl"),
  continuous_format = "mean_sd"
)

tbl_stats(summary_stats(mtcars, by = am))

}
