% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style_table.R
\name{style_table}
\alias{style_table}
\title{Style a GTstats table}
\usage{
style_table(
  x,
  theme = c("default", "journal", "classic", "minimal", "compact"),
  title = NULL,
  subtitle = NULL,
  source_note = NULL,
  col_labels = NULL,
  row_labels = NULL,
  level_labels = NULL,
  align = NULL,
  hide_cols = NULL,
  bold_cols = NULL,
  italic_cols = NULL,
  font_size = NULL,
  row_striping = NULL,
  accent_color = NULL,
  stripe_color = NULL
)
}
\arguments{
\item{x}{A \code{gt_tbl} object, typically created with \code{\link[=tbl_stats]{tbl_stats()}}.}

\item{theme}{Table theme. One of \code{"default"}, \code{"journal"},
\code{"classic"}, \code{"minimal"}, or \code{"compact"}.}

\item{title}{Optional table title.}

\item{subtitle}{Optional table subtitle.}

\item{source_note}{Optional source note shown below the table.}

\item{col_labels}{Optional named vector used to rename columns.}

\item{row_labels}{Optional named vector used to replace values in
the \code{"Variable"} or \code{"Measure"} column.}

\item{level_labels}{Optional named vector used to replace values in
the \code{"Level"} column.}

\item{align}{Optional named list controlling column alignment, for
example \code{list(left = cols, right = cols, center = cols)}.}

\item{hide_cols}{Optional columns to hide.}

\item{bold_cols}{Optional columns to display in bold.}

\item{italic_cols}{Optional columns to display in italics.}

\item{font_size}{Optional table font size in pixels.}

\item{row_striping}{Logical; whether to apply row striping.}

\item{accent_color}{Optional accent colour used for borders.}

\item{stripe_color}{Optional background colour for striped rows.}
}
\value{
A styled \code{gt_tbl} object.
}
\description{
Apply visual styling to a \code{gt} table produced by \code{\link[=tbl_stats]{tbl_stats()}}.
}
\details{
This function modifies the appearance of a rendered \code{gtstats} table
without changing the underlying statistical results. It can be used
to:
\itemize{
\item apply a theme
\item add titles and source notes
\item relabel columns, rows, and levels
\item align, hide, bold, or italicise selected columns
\item apply row striping and accent colours
\item adjust overall font size
}

The input must already be a \code{gt_tbl}, so styling is applied after
calling \code{\link[=tbl_stats]{tbl_stats()}}.
}
\examples{
tbl_stats(
  descriptive_table(mtcars, by = am) |>
    add_summary(vars = c(mpg, wt)) |>
    add_p()
) |>
  style_table(theme = "journal")

tbl_stats(
  descriptive_table(mtcars, by = am, overall = TRUE) |>
    add_summary(vars = c(mpg, wt, cyl)) |>
    add_total()
) |>
  style_table(
    theme = "minimal",
    col_labels = c("am = 1" = "Manual", "am = 0" = "Automatic"),
    accent_color = "#123B7A"
  )

}
