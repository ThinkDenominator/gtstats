% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_summary.R
\name{add_summary}
\alias{add_summary}
\title{Add summary rows to a descriptive table}
\usage{
add_summary(
  x,
  vars,
  statistic = "recommended",
  percent = c("column", "row", "overall", "none"),
  categorical = c("n_percent", "n_over_N_percent", "n", "percent"),
  categorical_layout = c("combined", "separate"),
  overall_categorical = c("auto", "n_percent", "n_over_N_percent", "n", "percent"),
  show_dichotomous = c("all_levels", "single_row"),
  value = NULL,
  layout = NULL,
  missing = c("ifany", "always", "no", "as_category"),
  digits = 1
)
}
\arguments{
\item{x}{A \code{gtstats_summary} object created with \code{\link[=summary_table]{summary_table()}}.}

\item{vars}{Variables to summarise. Can be supplied as bare names or as a
character vector.}

\item{statistic}{Continuous summary selection. A single value
applies to all selected continuous variables. A named character vector can
select a different summary for each variable, for example
\code{c(age = "mean_sd", bmi = "median_iqr")}. \code{"auto"} is accepted as an alias
for \code{"recommended"}.}

\item{percent}{Denominator for categorical percentages: \code{"column"} uses the
non-missing denominator within each group, \code{"row"} distributes each level
across groups, \code{"overall"} uses the overall non-missing denominator, and
\code{"none"} displays counts only.}

\item{categorical}{Display for categorical values: \code{"n_percent"},
\code{"n_over_N_percent"}, \code{"n"}, or \code{"percent"}.}

\item{categorical_layout}{Categorical display layout. \code{"combined"} keeps n
and \% together. \code{"separate"} creates distinct n and \% child columns and is
available for categorical-only tables without confidence intervals.}

\item{overall_categorical}{Categorical display used only in the Overall
column. \code{"auto"} uses counts only when \code{percent = "row"} and otherwise
follows \code{categorical}. Other choices are \code{"n_percent"},
\code{"n_over_N_percent"}, \code{"n"}, and \code{"percent"}.}

\item{show_dichotomous}{How binary variables are displayed. \code{"all_levels"}
(default) shows both levels. \code{"single_row"} shows one event level as a
compact row using the variable label.}

\item{value}{Optional named character vector or named list selecting the
event level used when \code{show_dichotomous = "single_row"}, for example
\code{c(smoke = "Yes", hypertension = "Yes")}. When omitted, the second
declared factor level (or the second sorted observed value) is used.}

\item{layout}{Table layout. \code{"compact"} keeps each summary in one cell;
\code{"separate"} places summaries and confidence intervals in separate
columns once intervals are added. It does not create empty CI columns.
When omitted, the layout chosen in \code{\link[=summary_table]{summary_table()}} is used.}

\item{missing}{Missing-value display and percentage handling. \code{"ifany"}
shows a missing row only when needed, \code{"always"} always shows it, and
\code{"no"} hides it; these three use non-missing categorical denominators.
\code{"as_category"} displays missing values as a category and includes them
when calculating categorical percentages.}

\item{digits}{One number applied throughout, or a named numeric vector using
\code{continuous}, \code{percent}, and \code{ci}.}
}
\value{
An updated \code{gtstats_summary} object with summary rows added.
}
\description{
Add summary statistics to a \code{gtstats} descriptive table builder.
}
\details{
This function is the main way to populate a descriptive table with variable
summaries. It supports both grouped and ungrouped tables and can optionally
add an \code{Overall} column when the descriptive table was created with
\code{overall = TRUE}.

Continuous variables can be displayed in one of four formats:
\itemize{
\item \code{"recommended"}: mean (SD) or median (IQR) as appropriate
\item \code{"mean_sd"}: mean (SD)
\item \code{"mean_se"}: mean (standard error)
\item \code{"mean_ci"}: mean with a t confidence interval
\item \code{"median_iqr"}: median (IQR)
\item \code{"both"}: mean (SD) and median (IQR)
}

Variable names may be supplied either as bare names, for example
\code{c(age, sex, bmi)}, or as a character vector, for example
\code{c("age", "sex", "bmi")}.
}
\examples{
summary_table(mtcars, by = am) |>
  add_summary(vars = c(mpg, wt, cyl))

summary_table(mtcars, by = am, overall = TRUE) |>
  add_summary(vars = c("mpg", "wt", "cyl"))

summary_table(mtcars) |>
  add_summary(vars = c(mpg, wt), statistic = "mean_sd")

missing_example <- mtcars
missing_example$vs[1:3] <- NA
summary_table(missing_example) |>
  add_summary(vars = vs, missing = "as_category")

}
