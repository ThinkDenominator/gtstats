% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/descriptive_table.R
\name{descriptive_table}
\alias{descriptive_table}
\title{Create a descriptive table builder}
\usage{
descriptive_table(
  data,
  by = NULL,
  mode = c("summary", "rate"),
  overall = FALSE
)
}
\arguments{
\item{data}{A data.frame.}

\item{by}{Optional grouping variable. Can be supplied as a bare name
or as a character string. The grouping variable must be
categorical, binary, or ordinal.}

\item{mode}{Table mode. One of \code{"summary"} or \code{"rate"}.}

\item{overall}{Logical; whether an overall column should be included
later when supported by downstream helpers.}
}
\value{
A \code{gt_desc_table} object containing the source data,
structural settings, and placeholders for table components.
}
\description{
Initialise a \code{gtstats} descriptive table object that can be built
step by step using helper functions such as \code{\link[=add_summary]{add_summary()}},
\code{\link[=add_proportion]{add_proportion()}}, \code{\link[=add_rate]{add_rate()}}, \code{\link[=add_total]{add_total()}}, and \code{\link[=add_p]{add_p()}}.
}
\details{
This function creates the table builder object but does not yet add
any rows. Rows are appended later through the \verb{add_*()} helpers,
and the finished object can be rendered with \code{\link[=tbl_stats]{tbl_stats()}}.

Two modes are supported:
\itemize{
\item \code{"summary"} for baseline tables and descriptive summaries
\item \code{"rate"} for rate-based tables using \code{\link[=add_rate]{add_rate()}}
}

A grouping variable may be supplied to create one column per group.
An optional \code{overall} column can also be requested for later use.
}
\examples{
descriptive_table(mtcars)

descriptive_table(mtcars, by = am)

descriptive_table(mtcars, by = am, overall = TRUE)

descriptive_table(mtcars, mode = "rate")

}
