% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_row.R
\name{add_row}
\alias{add_row}
\title{Add a custom row to a descriptive table}
\usage{
add_row(x, label, overall = NULL, values = NULL, level = "")
}
\arguments{
\item{x}{A \code{gt_desc_table} object created with \code{\link[=summary_table]{summary_table()}}.}

\item{label}{A single character string giving the row label to display in the
\code{Variable} column.}

\item{overall}{Optional value to display in the \code{Overall} column when the
descriptive table includes \code{overall = TRUE}.}

\item{values}{Optional named character vector or named list giving values for
displayed group columns. Names must match the displayed group column names,
for example \code{c("am = 1" = "2020-2024", "am = 0" = "2020-2024")}.}

\item{level}{Optional text to display in the \code{Level} column. Defaults to an
empty string.}
}
\value{
An updated \code{gt_desc_table} object with the custom row appended.
}
\description{
Add a user-defined row to a \code{gtstats} descriptive table. This is useful for
inserting contextual information such as study period, data source, setting,
or other custom annotations that should appear alongside the main table
content.
}
\details{
The row is matched to the current table structure automatically:
\itemize{
\item if \code{overall = TRUE}, an \code{Overall} column is supported
\item if \code{by} is used, values must be named using displayed group column names
\item if neither \code{overall} nor \code{by} is used, a single \code{Value} column is used
}
}
\examples{
res <- summary_table(mtcars, by = am, overall = TRUE) |>
  add_summary(vars = c(mpg, wt)) |>
  add_row(
    label = "Study period",
    overall = "2020-2024",
    values = c("am = 1" = "2020-2024", "am = 0" = "2020-2024")
  )

}
