% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assess_distribution.R
\name{assess_distribution}
\alias{assess_distribution}
\title{Assess the empirical distribution of continuous variables}
\usage{
assess_distribution(
  data,
  vars = NULL,
  by = NULL,
  normality_test = TRUE,
  skew_cutoff = 1,
  min_n = 3,
  plots = FALSE,
  digits = 2,
  format = c("table", "tibble"),
  output = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{vars}{Continuous numeric variables to assess. Bare names or a character
vector are accepted. When omitted, all detected continuous variables are
assessed. Categorical, ordinal, logical, date-time, and binary variables
are rejected when explicitly selected.}

\item{by}{Optional grouping variable, supplied as a bare name or character
string. Factors, characters, logical variables, binary variables, and
ordinal variables are supported.}

\item{normality_test}{Logical; run Shapiro-Wilk when 3 to 5000 finite
observations are available. Default is \code{TRUE}.}

\item{skew_cutoff}{Positive absolute-skewness threshold for marked skew.}

\item{min_n}{Minimum finite observations required for a shape assessment.}

\item{plots}{Logical; create histogram, density, Q-Q, and box plots. Plots
are stored in \verb{$plots} (or \code{attr(result, "plots")} for tibble output).}

\item{digits}{Number of decimal places.}

\item{format}{Output format: \code{"table"} (default) or \code{"tibble"}.}

\item{output}{Compatibility alias for \code{format}.}
}
\value{
With \code{format = "table"}, a \code{gt_distribution} object that prints as a
publication-ready table. \verb{$summary} contains group-level diagnostics and
\verb{$recommendations} contains one descriptive recommendation per variable.
With \code{format = "tibble"}, the group-level summary tibble is returned.
}
\description{
Assess the empirical distribution of continuous numeric variables to support
descriptive reporting. The function describes missingness, finite sample
size, skewness, and (optionally) Shapiro-Wilk results. It provides guidance
about presenting a variable; it does \strong{not} select an inferential test.
}
\details{
When \code{by} is supplied, diagnostics are calculated within every group and one
consistent, variable-level recommendation is also returned in
\verb{$recommendations}. Shapiro-Wilk is supporting information only: it is
sensitive to sample size and never determines the recommendation by itself.
}
\examples{
assess_distribution(mtcars, vars = c(mpg, wt))
assess_distribution(mtcars, vars = c(mpg, wt), by = am)
assess_distribution(mtcars, vars = "mpg", normality_test = FALSE)
assess_distribution(mtcars, vars = c("mpg", "wt"), plots = TRUE)$plots

}
