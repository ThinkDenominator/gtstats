% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/twobytwo_table.R
\name{twobytwo_table}
\alias{twobytwo_table}
\title{Create a 2x2 epidemiology table}
\usage{
twobytwo_table(
  data,
  exposure,
  outcome,
  exposed_level = NULL,
  outcome_level = NULL,
  measures = c("risk", "rr", "or", "rd"),
  conf.level = 0.95,
  digits = 1,
  test = c("auto", "chisq", "fisher"),
  output = c("table", "tibble", "both"),
  quiet = FALSE
)
}
\arguments{
\item{data}{A data.frame.}

\item{exposure}{Exposure variable. Can be supplied as a bare name or
as a character string.}

\item{outcome}{Outcome variable. Can be supplied as a bare name or
as a character string.}

\item{exposed_level}{Optional exposed level used to define the
exposed group.}

\item{outcome_level}{Optional outcome level used to define the
event.}

\item{measures}{Character vector of measures to include. Must contain
one or more of \code{"risk"}, \code{"rr"}, \code{"or"}, or \code{"rd"}.}

\item{conf.level}{Confidence level for intervals. Default is \code{0.95}.}

\item{digits}{Number of decimal places used for formatting.}

\item{test}{Statistical test to use. One of \code{"auto"}, \code{"chisq"}, or
\code{"fisher"}.}

\item{output}{Output style. Currently stored in the returned object.}

\item{quiet}{Logical; suppress messages.}
}
\value{
A \code{gt_twobytwo} object containing:
\itemize{
\item \code{inputs} — function inputs and settings
\item \code{summary} — detailed summary table
\item \code{table} — display-ready 2x2 results table
\item \code{notes} — explanatory notes
\item \code{call} — matched function call
}
}
\description{
Compute a 2x2 table for a binary exposure and a binary outcome,
including risks, risk ratio, odds ratio, risk difference, and a
p-value.
}
\details{
This function is intended for simple epidemiological analyses where
both exposure and outcome have exactly two non-missing levels.
Users may optionally specify which exposure level should be treated
as "exposed" and which outcome level should be treated as the event.

The returned object contains both a detailed summary and a compact,
display-ready table. By default, the function reports:
\itemize{
\item risks in exposed and unexposed groups
\item risk ratio
\item odds ratio
\item risk difference
\item a p-value from either chi-square or Fisher's exact test
}

Continuity correction is applied automatically for ratio measures
when zero cells are present.
}
\examples{
twobytwo_table(mtcars, exposure = am, outcome = vs)

twobytwo_table(
  mtcars,
  exposure = am,
  outcome = vs,
  measures = c("risk", "rr", "or")
)

tbl_stats(twobytwo_table(mtcars, exposure = am, outcome = vs))

}
