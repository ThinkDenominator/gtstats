% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tbl_stats.R
\name{tbl_stats}
\alias{tbl_stats}
\title{Create formatted gt tables from gtstats objects}
\usage{
tbl_stats(
  x,
  title = NULL,
  subtitle = NULL,
  digits = NULL,
  pvalue_style = c("default", "scientific"),
  bold_labels = TRUE,
  show_footnotes = TRUE
)
}
\arguments{
\item{x}{A supported \code{gtstats} object.}

\item{title}{Optional table title.}

\item{subtitle}{Optional table subtitle.}

\item{digits}{Optional digits argument reserved for future use.}

\item{pvalue_style}{P-value display style. Currently stored but
reserved for future formatting extensions.}

\item{bold_labels}{Logical; whether to bold variable labels where
appropriate.}

\item{show_footnotes}{Logical; whether explanatory footnotes should
be displayed.}
}
\value{
A \code{gt_tbl} object.
}
\description{
Render supported \code{gtstats} objects as formatted \code{gt} tables.
}
\details{
This function is the main rendering bridge between analytical
\code{gtstats} objects and presentation-ready table output. It supports
descriptive, inferential, epidemiological, and table-builder
objects created by the package and applies a consistent visual
style using \code{gt}.

Supported inputs include:
\itemize{
\item \code{gt_describe}
\item \code{gt_distribution}
\item \code{gt_variance}
\item \code{gt_compare}
\item \code{gt_correlation}
\item \code{gt_effect}
\item \code{gt_prop}
\item \code{gt_rate}
\item \code{gt_twobytwo}
\item \code{gt_desc_table}
}
}
\examples{
tbl_stats(describe_data(mtcars))

tbl_stats(summary_table(mtcars, by = am, include = c(mpg, wt)))

tbl_stats(compare_groups(mtcars, variable = mpg, group = am))

tbl_stats(
  summary_table(mtcars, by = am, overall = TRUE) |>
    add_summary(vars = c(mpg, wt, cyl)) |>
    add_total() |>
    add_p()
)

}
