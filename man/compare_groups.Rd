% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_groups.R
\name{compare_groups}
\alias{compare_groups}
\title{Compare groups using common inferential tests}
\usage{
compare_groups(
  data,
  outcome,
  group,
  paired = FALSE,
  test = c("auto", "t_test", "welch_t", "wilcox", "anova", "kruskal", "chisq", "fisher",
    "mcnemar"),
  normality_check = FALSE,
  var_equal = FALSE,
  correction = TRUE,
  effect_size = FALSE,
  conf.level = 0.95,
  digits = 3,
  output = c("table", "tibble", "both"),
  quiet = FALSE
)
}
\arguments{
\item{data}{A data.frame.}

\item{outcome}{Outcome variable. Can be supplied as a bare name or
as a character string.}

\item{group}{Grouping variable. Can be supplied as a bare name or as
a character string.}

\item{paired}{Logical; whether the comparison is paired. If \code{TRUE},
paired t-test, Wilcoxon signed-rank test, or McNemar test will be
used where appropriate.}

\item{test}{Test to use. One of \code{"auto"}, \code{"t_test"}, \code{"welch_t"},
\code{"wilcox"}, \code{"anova"}, \code{"kruskal"}, \code{"chisq"}, \code{"fisher"}, or
\code{"mcnemar"}.}

\item{normality_check}{Logical; currently reserved for future
versions.}

\item{var_equal}{Logical; whether equal variances should be assumed
for two-sample t-tests.}

\item{correction}{Logical; whether continuity correction should be
used for chi-square tests where relevant.}

\item{effect_size}{Logical; whether effect size should be calculated
where supported.}

\item{conf.level}{Confidence level for intervals.}

\item{digits}{Number of decimal places for formatting.}

\item{output}{Output style. Currently stored in the returned object.}

\item{quiet}{Logical; suppress messages.}
}
\value{
A \code{gt_compare} object containing:
\itemize{
\item \code{inputs} — function inputs and settings
\item \code{descriptives} — descriptive summaries by group
\item \code{inferential} — inferential test results
\item \code{table} — display-ready results table
\item \code{method} — metadata on detected variable types
\item \code{notes} — explanatory notes
\item \code{call} — matched function call
}
}
\description{
Compare an outcome across groups using a practical set of common
inferential tests.
}
\details{
This function is designed for beginner-friendly and teaching-focused
workflows. It combines:
\itemize{
\item descriptive summaries by group
\item automatic or user-specified test selection
\item effect size calculation where supported
\item a simple display-ready results table
}

Supported outcome types are:
\itemize{
\item continuous
\item binary
\item categorical
\item ordinal
}

Supported tests include:
\itemize{
\item \code{"auto"}
\item \code{"t_test"}
\item \code{"welch_t"}
\item \code{"wilcox"}
\item \code{"anova"}
\item \code{"kruskal"}
\item \code{"chisq"}
\item \code{"fisher"}
\item \code{"mcnemar"}
}

In \code{test = "auto"} mode:
\itemize{
\item continuous outcomes with 2 groups use a t-test or Welch t-test
\item continuous outcomes with >2 groups use ANOVA
\item categorical outcomes use chi-square or Fisher's exact test
\item paired binary outcomes use McNemar test
}

When \code{effect_size = TRUE}, effect sizes are returned where supported.
}
\examples{
compare_groups(mtcars, outcome = mpg, group = am)

compare_groups(
  mtcars,
  outcome = mpg,
  group = am,
  effect_size = TRUE
)

compare_groups(
  mtcars,
  outcome = vs,
  group = am,
  test = "chisq",
  effect_size = TRUE
)

compare_groups(
  mtcars,
  outcome = mpg,
  group = am,
  paired = FALSE,
  test = "welch_t"
)

tbl_stats(compare_groups(mtcars, outcome = mpg, group = am))

}
