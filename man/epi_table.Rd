% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epi_table.R
\name{epi_table}
\alias{epi_table}
\title{Outbreak and surveillance summary table}
\usage{
epi_table(
  data,
  outcomes = NULL,
  by = NULL,
  event = NULL,
  numerator = NULL,
  denominator = NULL,
  label = NULL,
  person_time = NULL,
  measure = c("proportion", "prevalence", "attack_rate", "incidence_rate"),
  multiplier = NULL,
  ci_method = c("wilson", "exact"),
  conf.level = 0.95,
  p_value = FALSE,
  p_adjust = c("none", "holm", "bonferroni", "BH", "fdr"),
  effects = "none",
  layout = c("auto", "wide", "long"),
  digits = 1,
  format = c("table", "tibble")
)
}
\arguments{
\item{data}{A data frame.}

\item{outcomes}{Line-list outcome variables selected with tidyselect syntax.}

\item{by}{Optional grouping variable.}

\item{event}{Event level to count. Supply one value for all outcomes or a
named vector, for example \code{c(infected = "Yes", admitted = "Yes")}.}

\item{numerator, denominator}{Aggregate count and denominator columns. For an
incidence rate, \code{denominator} is accumulated person-time.}

\item{label}{Optional aggregate outcome-label column or a single text label.}

\item{person_time}{Optional line-list person-time column. Required when
\code{measure = "incidence_rate"}.}

\item{measure}{One of \code{"proportion"}, \code{"prevalence"}, \code{"attack_rate"}, or
\code{"incidence_rate"}.}

\item{multiplier}{Scale used for estimates. Defaults to 100 for proportions,
prevalence and attack rates, and 1,000 for incidence rates.}

\item{ci_method}{Binomial interval method: \code{"wilson"} or \code{"exact"}.
Incidence rates always use an exact Poisson interval.}

\item{conf.level}{Confidence level.}

\item{p_value}{Add an association/rate-comparison p-value when \code{by} is used.}

\item{p_adjust}{Optional multiplicity adjustment passed to \code{\link[stats:p.adjust]{stats::p.adjust()}}.}

\item{effects}{Optional two-group effect measures. Use \code{"none"} (default),
\code{"all"}, or any of \code{"rr"}, \code{"rd"}, and \code{"or"}. Incidence-rate tables
support \code{"irr"}.}

\item{layout}{\code{"auto"}, \code{"wide"}, or \code{"long"}. Auto uses wide output for
four or fewer groups and long output otherwise.}

\item{digits}{Number of decimal places.}

\item{format}{\code{"table"} (default) or \code{"tibble"}.}
}
\value{
A \code{gt_epi_table} object containing \verb{$summary}, \verb{$table},
\verb{$denominators}, \verb{$p_values}, \verb{$effects}, \verb{$inputs}, and \verb{$notes}.
}
\description{
Build a publication-ready epidemiology table from either individual
line-list records or aggregate numerator/denominator data. Unlike
\code{\link[=summary_table]{summary_table()}}, this function always makes the event and denominator
explicit and always reports a confidence interval.
}
\examples{
epi_table(
  birthwt, outcomes = low, by = smoke,
  event = "Low birth weight", measure = "prevalence"
)

aggregate_data <- data.frame(
  ward = c("A", "B"), cases = c(12, 7), population = c(80, 65)
)
epi_table(
  aggregate_data, numerator = cases, denominator = population,
  by = ward, label = "Influenza", measure = "attack_rate"
)
}
